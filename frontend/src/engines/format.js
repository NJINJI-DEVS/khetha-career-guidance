// Extracted from App.jsx (Stage 2 of the App.jsx split — see
// plans/nested-churning-hellman.md). Moved verbatim, no logic changes.

export const fmt = (n) => n.toLocaleString("en-ZA");
export const pct = (n) => `${Math.round(n * 100)}%`;
